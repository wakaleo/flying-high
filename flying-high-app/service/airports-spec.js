describe('Airport', function() {

  beforeEach(module('flying-high-app'));

  it('should retrieve all known airports', inject(function(Airport) {

	//expect(airports.doSomething()).toEqual('something');

  }));

});